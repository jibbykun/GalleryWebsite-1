<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8", name="viewport" content="width=device-width, initial-scale=1">
<title>Item Details</title><html>
<link rel="stylesheet" type="text/css" href="style.css">

<body>
    	{{#if successMsg}}
 			<p class="msg-success">{{successMsg}}</p>
		{{/if}}
        {{#if errorMsg}}
 			<p class="msg-danger">{{errorMsg}}</p>
		{{/if}}

    <section class="container">

        <nav class="side">
            <a href="about">About</a> |
            <a href="#services">Services</a> |
            <a href="login">Login</a> |
            <a href="register">Sign Up</a> |
            <a href="#contact">Contact</a> |
        </nav>

        <h1>Item Details</h1>
        <div class="gallery">
        <a target="_blank" href="{{imageDir}}">
            <img src="{{imageDir}}" alt="No picture found" width="600" height="400">
        </a>
        <div class="desc">Name: {{item}} Price: {{price}}</div>
        </div>

        <h1>Seller Details</h1>
        <p>Username: {{username}}</p>
        <p>Current Profile Picture</p>
          <a target="_blank" href="{{picDir}}">
             <img src="{{picDir}}" alt="No profile picture" width="600" height="400">
         </a>

    </section>
</body>

<footer> 
    <br>
    <br>
    <a href="http://localhost:8080">Click here to go Home</a>
</footer>
</html>